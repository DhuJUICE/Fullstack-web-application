from django.shortcuts import render

# Create your views here.
#search document resources based on subjects or keywords, or search in subject based on keywords
def resourceSearch(request):
	#get the resources from database based on subject
	
	#get the resources from database based on keywords

	#get the resources from database based on subject and keywords

	#return the page with the search results
	return None